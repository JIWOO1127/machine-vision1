# data/

- `raw/` : 배치 모드 입력용 원본 강의실 표지판 사진 (git에는 커밋하지 않음, .gitkeep만 유지)
- `processed/` : 필요 시 전처리(리사이즈, 원근 보정 등)된 이미지
- `annotations/` : YOLO 학습용 라벨(txt) — 클래스별 bbox 좌표

용량이 큰 실제 데이터는 저장소에 올리지 않고, 별도 클라우드 스토리지나
개인 드라이브에서 관리한 뒤 필요할 때 이 폴더로 복사해서 사용하세요.
